═══════════════════════════════════════════════════════════════════
🚀 نشر Mubsat AI - الأوامر النهائية (انسخ والصق!)
═══════════════════════════════════════════════════════════════════

📍 الخطوة 1: اتصل بالسيرفر
────────────────────────────────────────────────────────────────────

ssh root@46.202.159.100


📍 الخطوة 2: انسخ والصق هذا Script كاملاً
────────────────────────────────────────────────────────────────────

bash << 'DEPLOY_SCRIPT'
set -e
export PATH=/root/.nvm/versions/node/v24.11.0/bin:$PATH

# Generate password
DB_PASS=$(openssl rand -base64 32 | tr -d "=+/" | cut -c1-25)
echo "🔐 Password: $DB_PASS (احفظها!)"

# Install PM2
npm install -g pm2 2>/dev/null || true

# PostgreSQL
sudo systemctl start postgresql
sudo systemctl enable postgresql

# Create database
sudo -u postgres psql << EOF
CREATE DATABASE mubsat_ai;
CREATE USER mubsat_user WITH PASSWORD '$DB_PASS';
GRANT ALL PRIVILEGES ON DATABASE mubsat_ai TO mubsat_user;
\q
EOF

# Setup app directory
mkdir -p /var/www/mubsat-ai
cd /var/www/mubsat-ai

# Create .env
cat > .env << EOF
NODE_ENV=production
PORT=5000
DATABASE_URL=postgresql://mubsat_user:$DB_PASS@localhost:5432/mubsat_ai
SESSION_SECRET=$(openssl rand -base64 32)
TELEGRAM_BOT_TOKEN=your_bot_token_here
EOF

# PM2 config
cat > ecosystem.config.js << 'EOF'
module.exports = {
  apps: [{
    name: 'mubsat-ai',
    script: './dist/server/index.js',
    instances: 2,
    exec_mode: 'cluster',
    env: { NODE_ENV: 'production', PORT: 5000 }
  }]
};
EOF

# Nginx
sudo tee /etc/nginx/conf.d/mubsat-ai.conf > /dev/null << 'EOF'
server {
    listen 80;
    server_name 46.202.159.100;
    client_max_body_size 50M;
    location / {
        proxy_pass http://localhost:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
    }
}
EOF
sudo nginx -t && sudo systemctl reload nginx

echo "✅ السيرفر جاهز! الآن ارفع الملفات إلى /var/www/mubsat-ai"
DEPLOY_SCRIPT


📍 الخطوة 3: ارفع ملفات التطبيق
────────────────────────────────────────────────────────────────────

طريقة A (الأسرع - من Replit):
────────────────────────────────
افتح Terminal جديد في Replit وشغّل:

tar czf - --exclude=node_modules --exclude=.git --exclude=dist . | \
  ssh root@46.202.159.100 'cd /var/www/mubsat-ai && tar xzf -'


طريقة B (يدوياً):
──────────────────
انسخ هذه المجلدات/الملفات من Replit إلى /var/www/mubsat-ai:
✓ package.json
✓ server/
✓ client/
✓ shared/
✓ drizzle.config.ts
✓ vite.config.ts
✓ tsconfig.json
✓ tailwind.config.ts
✓ postcss.config.js


📍 الخطوة 4: بناء وتشغيل التطبيق
────────────────────────────────────────────────────────────────────

بعد رفع الملفات، في السيرفر SSH شغّل:

cd /var/www/mubsat-ai
export PATH=/root/.nvm/versions/node/v24.11.0/bin:$PATH

npm install
npm run build
npm run db:push --force

pm2 delete mubsat-ai 2>/dev/null || true
pm2 start ecosystem.config.js
pm2 save
pm2 startup


📍 التحقق من النجاح
────────────────────────────────────────────────────────────────────

pm2 status
pm2 logs mubsat-ai

افتح المتصفح:
http://46.202.159.100


═══════════════════════════════════════════════════════════════════
✅ انتهى! التطبيق سيعمل على http://46.202.159.100
═══════════════════════════════════════════════════════════════════

📝 أوامر مفيدة:
  • pm2 restart mubsat-ai  (إعادة تشغيل)
  • pm2 logs mubsat-ai     (عرض اللوقات)
  • pm2 stop mubsat-ai     (إيقاف)
  • pm2 status             (الحالة)
